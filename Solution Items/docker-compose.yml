version: '3.8'

services:
  # database section
  sqlserverdb:
    image: mcr.microsoft.com/mssql/server:2019-latest

# network section
networks:
  default:
    name: java_microservices
    driver: bridge

# volumes section
volumes:
  sqlserver-data:
    name: sqlserver_data
