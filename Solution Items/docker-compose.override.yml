version: '3.8'

services:
  orderdb:
    container_name: orderdb
    environment:
      SA_PASSWORD: "Passw0rd!"
      ACCEPT_EULA: "Y"
    restart: always
    volumes:
      - sqlserver-data:/var/lib/sqlserver/data
    ports:
      - "1435:1433"
